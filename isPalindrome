func isPalindrome(x int) bool {
	// 特殊情况：
	// 1. 负数不是回文数
	// 2. 最后一位是0的数，只有0本身是回文数
	if x < 0 || (x % 10 == 0 && x != 0) {
		return false
	}

	revertedNumber := 0
	for x > revertedNumber {
		revertedNumber = revertedNumber * 10 + x % 10
		x /= 10
	}

	// 当数字长度为奇数时，我们可以通过 revertedNumber/10 去掉中间的数字
	// 例如，当输入为 12321 时，在循环结束时 x = 12，revertedNumber = 123
	// 由于中间的数字不影响回文（它总是与自己相等），所以我们可以将其去掉
	return x == revertedNumber || x == revertedNumber / 10
}

func plusOne(digits []int) []int {
	// 从最后一位开始处理
	for i := len(digits) - 1; i >= 0; i-- {
		if digits[i] < 9 {
			// 当前位不是9，直接加1返回
			digits[i]++
			return digits
		}
		// 当前位是9，变0，继续处理前一位
		digits[i] = 0
	}

	// 如果所有位都是9（如 999 → 1000）
	// 在数组最前面插入1
	return append([]int{1}, digits...)
}
